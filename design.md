# Design Document

## Overview
ไฟล์นี้สรุปแนวทางการออกแบบหน้าเว็บระบบจัดการน้ำประปาหมู่บ้าน หลังการปรับแต่ง UI เพื่อให้ใช้ง่ายขึ้นและดูสวยงามขึ้น

## Design Goals
- ทำให้ข้อมูลความขุ่นของน้ำมองเห็นได้ชัดเจนทันที
- ใช้สีบอกระดับความขุ่น เพื่อให้สถานะเข้าใจง่าย
- เพิ่มปุ่ม action บนหน้าเพจต่าง ๆ เพื่อเข้าถึงฟังก์ชันสำคัญได้เร็ว
- จัดวางหน้าเว็บให้ไม่ต้องเลื่อนนาน และรองรับการอ่านบนหน้าจอขนาดต่าง ๆ

## Color & Visual Style
- พื้นหลังหลัก (`container`) ใช้ gradient โทนฟ้าอ่อน เพื่อให้ความรู้สึกสดชื่นและสะอาด
- การ์ดหลัก (`card`) ใช้ขอบมนและเงาเบา เพื่อให้หน้าเว็บมีความทันสมัยและอ่านง่าย
- ปุ่มหลัก (`submitButton`, `actionButton`) ใช้สีฟ้าเข้ม เพื่อให้เด่นและใช้งานง่าย
- ปุ่มรอง (`secondaryButton`) ใช้พื้นหลังขาวและกรอบ เพื่อให้เป็นปุ่มนำทางที่ชัดเจนแต่ไม่แย่งความสำคัญ

## Turbidity Status
ระดับความขุ่นจะถูกแสดงด้วย:
- ใสสะอาด: NTU < 5
- ค่อนข้างใส: 5 <= NTU < 10
- ขุ่นเล็กน้อย: 10 <= NTU < 20
- ขุ่นมาก: NTU >= 20

การแสดงผลประกอบด้วย:
- ป้ายสถานะสี
- แถบวัดความขุ่น (`turbidity bar`) ที่เติมสีตามระดับ
- ข้อมูล NTU, เกณฑ์เตือน และข้อความสถานะ

## Page Improvements
### หน้า Water
- เพิ่มปุ่ม `รีเฟรชข้อมูล` และ `ดูประวัติการล้างถัง`
- แสดงการ์ดสรุปสถานะน้ำพร้อมสีสถานะ
- แสดงข้อมูลการแจ้งเตือนและข้อมูลผู้ดูแลในบล็อกแยกกัน

### หน้า Index
- เพิ่มส่วน header ที่สวยขึ้น
- เพิ่มปุ่มลัด `ดูสถานะน้ำ` และ `เข้าใช้งานประชาชน`
- จัดการ์ดลิงก์เป็นกริดที่อ่านง่ายและโมเดิร์น

### หน้า Maintenance
- เพิ่มปุ่ม `รีเฟรชรายการ` และปุ่มลิงก์ไปหน้า History
- แสดงการ์ดบันทึกการบำรุงรักษาแบบกริด

### หน้า History
- เพิ่มปุ่ม `รีเฟรชประวัติ` และลิงก์ไปหน้า Admin
- จัดข้อมูลให้อยู่ในบล็อกอ่านง่าย

### หน้า User
- แสดงปุ่มรีเฟรชและลิงก์ไปหน้า Contact
- เพิ่มปุ่มนำทางไปยังฟอร์มร้องเรียนโดยตรง
- แยกบล็อก Water Status, Alerts, Maintenance summary, Contact

### หน้า Contact
- เพิ่มปุ่มนำทางไปหน้า User และ Admin
- จัดฟอร์มให้สวยและอ่านง่าย

### หน้า Admin
- เพิ่มปุ่มรีเฟรชข้อมูล
- แยกบล็อกแต่ละส่วนให้ชัดเจน
- เพิ่มปุ่มลัดไปหน้า Water, Maintenance, User
- เพิ่มปุ่มเลื่อนลงสู่ฟอร์มบันทึก

## Component / Layout Patterns
- `pageHeader`: หัวข้อและปุ่มสำคัญในแถวเดียว
- `buttonRow`: ปุ่มนำทางและ action ในตำแหน่งเดียว
- `gridTwo`: การ์ดสองคอลัมน์สำหรับหน้าข้อมูลสรุป
- `sectionTitle`: หัวข้อส่วนพร้อมปุ่ม action ย่อย

## Notes
- ไฟล์ CSS อยู่ที่ `frontend/styles/Home.module.css`
- หน้าเพจที่ได้รับการปรับปรุงหลัก: `frontend/pages/index.js`, `frontend/pages/water.js`, `frontend/pages/maintenance.js`, `frontend/pages/history.js`, `frontend/pages/user.js`, `frontend/pages/contact.js`, `frontend/pages/admin.js`
- ถ้าต้องการเพิ่มธีมสี หรือ dark mode ต่อไปได้ง่าย
