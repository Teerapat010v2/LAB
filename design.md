# Design Document

## Overview
ไฟล์นี้สรุปแนวทางการออกแบบหน้าเว็บสำหรับ **ระบบจัดการน้ำประปาหมู่บ้าน** ซึ่งเน้นหลักการออกแบบแบบ Clean & Minimalist และ Mobile-First เพื่อให้ประชาชนเข้าถึงและใช้งานได้ง่ายที่สุด

## Design Goals
- ทำให้ข้อมูลสถานะน้ำ (ความขุ่น) มองเห็นได้ชัดเจนทันที
- ใช้สีบอกระดับความขุ่นและสถานะต่างๆ เพื่อให้เข้าใจง่าย โดยใช้โทนสีพาสเทลที่ไม่แสบตา
- ทำให้ระบบดูน่าเชื่อถือ เป็นทางการแต่เข้าถึงง่าย (Professional yet Accessible)
- รองรับการใช้งานผ่านโทรศัพท์มือถือ (Mobile-First) เนื่องจากผู้ใช้ส่วนใหญ่เป็นชาวบ้าน

## Color & Visual Style
- **พื้นหลังหลัก (`--bg-main`)**: ใช้สีเทาอ่อน (`#f8fafc`) เพื่อความสะอาดตา (Clean Aesthetic) แทนการใช้ลวดลายหรือ Gradient ที่ดูลายตา
- **สีหลัก (`--primary`)**: ใช้สีน้ำเงินเข้ม (`#0369a1` Trustworthy Blue) เป็นสีหลักของปุ่มและหัวข้อ เพื่อให้ความรู้สึกมั่นคง ปลอดภัย และเป็นทางการ
- **การ์ด (`card`)**: ใช้การ์ดสีขาวทึบ (`#ffffff`) พร้อมขอบมน (Rounded Corners) และเงาตกกระทบแบบนุ่มนวล (Soft Shadow) เพื่อให้เนื้อหาลอยเด่นขึ้นมาและอ่านง่ายที่สุด
- **สถานะสี (Status Colors)**: ใช้โทนสีอ่อน (Pastel Background) ตัดกับตัวหนังสือสีเข้ม เช่น สีเขียวอ่อน/เขียวเข้ม, สีแดงอ่อน/แดงเข้ม

## Typography & Components
- **ฟอนต์**: ใช้ `Outfit` สำหรับหัวข้อ (Headings) เพื่อความทันสมัย และ `Inter` สำหรับเนื้อหา (Body) เพื่อให้อ่านง่าย
- **Mobile-First Buttons**: ปุ่มและช่องกรอกฟอร์มถูกออกแบบให้มีความสูงอย่างน้อย `48px` เพื่อให้เหมาะสมกับนิ้วสัมผัสบนหน้าจอมือถือ (Touch Targets)
- **Turbidity Meter**: แถบความขุ่นของน้ำถูกขยายความหนาให้เป็น `28px` เพื่อให้สังเกตเห็นระดับสีของน้ำได้ชัดเจนบนหน้าจอเล็กๆ

## Page Improvements
- **หน้าแรก (Index)**: รวมปุ่มทางลัดเข้าสู่ "ระบบบริการประชาชน" และ "ผู้ดูแลระบบ (Admin)" ไว้ด้านบนอย่างชัดเจน การ์ดในหน้านี้ถูกจัดเป็น Grid ที่อ่านง่าย
- **ระบบบริการประชาชน (User)**: หน้าสำหรับประชาชนที่รวมข้อมูลสถานะน้ำ การแจ้งเตือน และแบบฟอร์มร้องเรียนไว้ในหน้าเดียว ใช้ภาษาที่เป็นทางการและเข้าใจง่าย
- **แดชบอร์ดผู้ดูแลระบบ (Admin)**: หน้าแดชบอร์ดสำหรับเจ้าหน้าที่ที่รวมข้อมูลทั้งหมดไว้ในที่เดียว ทั้งการเพิ่มประวัติ ซ่อมบำรุง และจัดการข้อร้องเรียน
- **หน้าบันทึกต่างๆ (Maintenance / History)**: แสดงรายการประวัติการล้างถังด้วยรูปแบบการ์ดที่อ่านง่ายกว่ารูปแบบตารางบนมือถือ

## Component / Layout Patterns
- `pageHeader`: หัวข้อและคำอธิบายจัดกลุ่มกันพร้อมเส้นกั้นด้านล่าง เพื่อแยกส่วนนำทางออกจากเนื้อหาหลัก
- `gridTwo`: โครงสร้าง Grid สองคอลัมน์ที่จะถูกปรับให้ซ้อนกัน (Stack) เป็นหนึ่งคอลัมน์แบบอัตโนมัติบนหน้าจอมือถือ
- `alertCard`: การ์ดแสดงผลแจ้งเตือนและรายการย่อย ที่มีขนาดกะทัดรัดแต่ยังคงความสวยงาม

## Notes
- ไฟล์ CSS อยู่ที่ `frontend/styles/Home.module.css` และ `frontend/styles/globals.css`
- หน้าเพจหลัก: `frontend/pages/index.js`, `frontend/pages/user.js`, `frontend/pages/admin.js`, ฯลฯ
