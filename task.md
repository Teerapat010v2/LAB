# Task List

## เป้าหมายโปรเจกต์
สร้างระบบจัดการน้ำประปาหมู่บ้านที่สามารถตรวจสอบคุณภาพน้ำ บันทึกประวัติการล้างถัง และแจ้งเตือนการบำรุงรักษา

## งานหลัก
1. กำหนดโครงสร้างโปรเจกต์
   - สร้างโฟลเดอร์ `frontend` และ `backend`
   - ตั้งค่าไฟล์ `.gitignore` และ `.env.local`

2. สร้าง frontend ด้วย Next.js
   - สร้างหน้า Dashboard และหน้าแรก
   - สร้างคอมโพเนนต์หลัก: `Header`, `WaterStatus`, `MaintenanceLog`, `Alert`
   - ตั้งค่า Tailwind CSS หรือ CSS Modules
   - สร้าง API route สำหรับ `water`, `maintenance`, `history`

3. สร้าง backend (ถ้าจำเป็น)
   - สร้าง API service แยก เช่น Flask/FastAPI หรือ Express
   - สร้างโมเดลข้อมูลและฐานข้อมูล
   - สร้าง route สำหรับข้อมูลน้ำและการบำรุงรักษา

4. เชื่อมต่อ frontend กับ backend
   - ตั้งค่า `NEXT_PUBLIC_API_URL`
   - สร้าง `api-client` สำหรับเรียก API
   - ทดสอบการดึงข้อมูลและการส่งข้อมูล

5. ฟีเจอร์แจ้งเตือน
   - ตรวจสอบค่าความขุ่นของน้ำ
   - แจ้งเตือนเมื่อค่าผิดปกติหรือถึงเงื่อนไขการบำรุงรักษา
   - แสดงสถานะใน dashboard

6. ทดสอบและปรับปรุง
   - รัน `npm run dev`
   - ทดสอบหน้าจอและ API
   - แก้บั๊กและปรับ UX

## งานเสริม
- เชื่อมฐานข้อมูลจริง (PostgreSQL, MongoDB, Supabase)
- สร้างระบบล็อกอินผู้ใช้
- ปรับ UI ให้รองรับมือถือ
- ดีพลอยบน Vercel และโฮสต์ backend

